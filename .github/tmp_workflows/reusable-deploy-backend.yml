name: Reusable Deploy Backend

on:
  workflow_call:
    inputs:
      components:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: set_env
        name: Determine environment
        run: |
          echo "set-up step"
  deliver:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Output deploy logs
        run: |
          echo "component=${{ inputs.components }} to deploy to environment"
  deploy-backend:
    runs-on: ubuntu-latest
    needs: deliver
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Output deploy logs
        run: |
          echo "component=${{ inputs.components }} to deploy to environment"
